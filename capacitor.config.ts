import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'stev.petcons.app',
  appName: 'petcons4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
