import { PetlibellePipe } from './petlibelle.pipe';

describe('PetlibellePipe', () => {
  it('create an instance', () => {
    const pipe = new PetlibellePipe();
    expect(pipe).toBeTruthy();
  });
});
